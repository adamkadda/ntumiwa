meta {
  name: list composers
  type: http
  seq: 2
}

get {
  url: {{host}}/composers
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
