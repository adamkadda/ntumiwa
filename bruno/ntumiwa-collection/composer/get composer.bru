meta {
  name: get composer
  type: http
  seq: 1
}

get {
  url: {{host}}/composers/:id
  body: none
  auth: inherit
}

params:path {
  id: 1
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
