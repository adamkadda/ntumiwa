meta {
  name: delete composer
  type: http
  seq: 5
}

delete {
  url: {{host}}/composers/:id
  body: none
  auth: inherit
}

params:path {
  id: 
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
