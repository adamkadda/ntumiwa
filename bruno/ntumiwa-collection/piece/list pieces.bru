meta {
  name: list pieces
  type: http
  seq: 2
}

get {
  url: {{host}}/pieces
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
