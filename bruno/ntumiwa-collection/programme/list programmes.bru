meta {
  name: list programmes
  type: http
  seq: 2
}

get {
  url: {{host}}/programmes
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
