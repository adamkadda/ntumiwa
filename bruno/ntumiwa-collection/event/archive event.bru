meta {
  name: archive event
  type: http
  seq: 8
}

patch {
  url: {{host}}/events/:id/archive
  body: none
  auth: inherit
}

params:path {
  id: 7
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
