meta {
  name: delete event
  type: http
  seq: 5
}

delete {
  url: {{host}}/events/:id
  body: none
  auth: inherit
}

params:path {
  id: 1
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
