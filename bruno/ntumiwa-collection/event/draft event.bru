meta {
  name: draft event
  type: http
  seq: 6
}

patch {
  url: {{host}}/events/:id/draft
  body: none
  auth: inherit
}

params:path {
  id: 8
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
