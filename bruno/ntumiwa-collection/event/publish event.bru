meta {
  name: publish event
  type: http
  seq: 7
}

patch {
  url: {{host}}/events/:id/publish
  body: none
  auth: inherit
}

params:path {
  id: 8
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
