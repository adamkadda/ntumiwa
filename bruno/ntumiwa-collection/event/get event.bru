meta {
  name: get event
  type: http
  seq: 1
}

get {
  url: {{host}}/events/:id
  body: none
  auth: inherit
}

params:path {
  id: 8
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
