meta {
  name: list events
  type: http
  seq: 2
}

get {
  url: {{host}}/events
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
