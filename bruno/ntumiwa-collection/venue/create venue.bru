meta {
  name: create venue
  type: http
  seq: 3
}

post {
  url: {{host}}/venues
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "address": "foo"
  }
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
