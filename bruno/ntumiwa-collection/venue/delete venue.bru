meta {
  name: delete venue
  type: http
  seq: 5
}

delete {
  url: {{host}}/venues/:id
  body: none
  auth: inherit
}

params:path {
  id: 5
}

script:post-response {
  bru.setEnvVar("csrf-token", res.headers["x-csrf-token"]);
}

settings {
  encodeUrl: true
  timeout: 0
}
